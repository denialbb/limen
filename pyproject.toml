[project]
name = "limen"
version = "0.1.0"
description = "A agent collaboration framework with MCP"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }

