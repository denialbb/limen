# Review for Issue 004

**Status:** APPROVED

The `watchShutdown` routine has been updated to emit `syscall.SIGTERM` before the grace period starts, cleanly allowing the subprocess to terminate before escalating to `SIGKILL`. This correctly implements the specified lifecycle. Tests have also been updated to assert this signal. Good work.
